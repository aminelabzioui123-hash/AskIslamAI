# AskIslamAI - مساعدك الإسلامي الذكي

## 📖 نظرة عامة
موقع إسلامي متطور يستخدم الذكاء الاصطناعي (Claude AI) للإجابة على الأسئلة من أمهات الكتب الإسلامية.

## ✨ المميزات
- 🎨 تصميم Glassmorphism حديث على طريقة iOS 18
- 📱 متجاوب بالكامل مع جميع الأجهزة
- 🔐 نظام تسجيل دخول
- 📚 يستند على مصادر إسلامية موثوقة
- ✍️ خط IBM Plex Sans Arabic الاحترافي
- 🌙 واجهة إسلامية راقية

## 📁 ملفات المشروع
```
├── index.html          # الصفحة الرئيسية (Landing Page)
├── chat.html           # صفحة المحادثة
├── worker.js           # Cloudflare Worker للـ API
└── README.md           # هذا الملف
```

## 🚀 خطوات التفعيل

### 1. رفع الموقع على الهوستينغ (Namecheap)

#### الطريقة الأولى: عبر cPanel
1. دخل لـ cPanel ديال الهوستينغ ديالك
2. امشي ل **File Manager**
3. امشي ل مجلد `public_html`
4. ارفع الملفات:
   - `index.html`
   - `chat.html`
5. تأكد ان `index.html` فالجذر ديال `public_html`

#### الطريقة الثانية: عبر FTP
1. استخدم برنامج FileZilla
2. اتصل بالهوستينغ باستخدام بيانات FTP من Namecheap
3. ارفع الملفات لـ `public_html`

### 2. إعداد Cloudflare Worker

#### خطوات الإعداد:
1. **إنشاء Worker:**
   - امشي ل [Cloudflare Dashboard](https://dash.cloudflare.com)
   - اختار **Workers & Pages** من القائمة
   - كليكي على **Create application**
   - اختار **Create Worker**
   - سمّيه `askislamai-api`

2. **نسخ الكود:**
   - افتح ملف `worker.js`
   - انسخ كل الكود
   - الصقه في محرر الـ Worker

3. **إضافة API Key:**
   - امشي ل **Settings** → **Environment Variables**
   - زيد متغير جديد:
     - Name: `ANTHROPIC_API_KEY`
     - Value: [API Key ديالك من Anthropic]
   - أو بدل `YOUR_ANTHROPIC_API_KEY_HERE` مباشرة في الكود

4. **نشر الـ Worker:**
   - كليكي على **Save and Deploy**
   - انسخ الـ URL ديال الـ Worker (مثال: `https://askislamai-api.your-subdomain.workers.dev`)

### 3. ربط الـ Worker بالموقع

1. افتح ملف `chat.html` للتعديل
2. شوف السطر 521 تقريباً:
```javascript
const response = await fetch('/api/chat', {
```

3. بدلها ب:
```javascript
const response = await fetch('https://askislamai-api.your-subdomain.workers.dev', {
```

4. احفظ التغييرات و ارفع الملف من جديد

### 4. إعداد Cloudflare DNS (لو كملتي الخطوات من قبل)

تأكد ان الـ DNS Records صحيحة:
- **A Record**: اسم الدومين → IP الهوستينغ
- **CNAME Record**: www → اسم الدومين
- **Proxy Status**: مفعل (السحابة البرتقالية)

## 🔑 الحصول على Anthropic API Key

1. امشي ل [console.anthropic.com](https://console.anthropic.com)
2. سجل دخول أو أنشئ حساب
3. امشي ل **API Keys**
4. كليكي **Create Key**
5. انسخ الـ Key (تبدا بـ `sk-ant-...`)

⚠️ **مهم جداً:** لا تشارك الـ API Key مع أي حد!

## 🎨 تخصيص التصميم

### تغيير الألوان:
في ملفات HTML، شوف الـ `:root` variables:
```css
:root {
    --primary: #0a5f4f;      /* اللون الأساسي */
    --secondary: #2d8b74;    /* اللون الثانوي */
    --gold: #d4a574;         /* الذهبي */
}
```

### تغيير الخط:
الموقع يستخدم **IBM Plex Sans Arabic** من Google Fonts.
لو بغيتي تبدل الخط، بدل الـ link في الـ `<head>`.

## 📚 إضافة مصادر إسلامية

في ملف `chat.html`، شوف الـ `system prompt` (السطر 427):
```javascript
system: `أنت مساعد إسلامي متخصص...`
```

تقدر تزيد مصادر أخرى أو تعدل التعليمات.

## 🔒 نظام تسجيل الدخول

حالياً نظام تسجيل الدخول **وهمي** (Simulated).
للمشروع الحقيقي:
1. احتاج Backend (Node.js, PHP, Python...)
2. قاعدة بيانات (MySQL, PostgreSQL, MongoDB...)
3. JWT tokens للأمان

## 🐛 حل المشاكل الشائعة

### الموقع ما كيظهرش:
- تحقق من ان `index.html` في `public_html`
- تحقق من الـ DNS settings في Cloudflare
- استنى 24 ساعة للـ propagation

### الـ API ما كتخدمش:
- تحقق من الـ API Key في الـ Worker
- شوف الـ Console (F12) للـ errors
- تأكد من الـ CORS headers

### التصميم مكسور على الموبايل:
- امسح الـ cache ديال المتصفح
- تحقق من الـ `viewport` meta tag

## 📱 التوافق

الموقع متوافق مع:
- ✅ Chrome / Edge / Brave
- ✅ Safari (iOS & macOS)
- ✅ Firefox
- ✅ جميع الأجهزة (Desktop, Tablet, Mobile)

## 🤝 المساهمة

لو عندك أفكار للتحسين:
1. Fork المشروع
2. دير التعديلات
3. ابعث Pull Request

## 📄 الترخيص

هذا المشروع مفتوح المصدر للاستخدام التعليمي.

## 📞 الدعم

لو عندك مشكلة:
1. شوف قسم "حل المشاكل" فوق
2. افتح Issue على GitHub
3. تواصل معنا

---

**صُنع بـ ❤️ لخدمة الإسلام**

بِسْمِ اللَّهِ الرَّحْمَٰنِ الرَّحِيمِ
